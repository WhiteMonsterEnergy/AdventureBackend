package white.monster.energy.adventurebackend.employee;

// Enum for defining employee roles within the organization
public enum EmployeeRole {
    MANAGER,  // Employee with managerial responsibilities
    OPERATOR  // Employee responsible for operations
}
